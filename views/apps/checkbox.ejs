<script>
    var boxes = document.querySelectorAll("input[type=checkbox]");
    var indexesInput = document.querySelector("#indexesInput");

    function collectCheckBoxes(){
        var checked = [];
    	boxes.forEach(function(checkedTrue){
    	    if(checkedTrue.checked)
    	   checked.push(checkedTrue.id);
    	});
        console.log(checked);
        indexesInput.value = checked;
    }
</script>